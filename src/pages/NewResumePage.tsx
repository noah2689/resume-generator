/**
 * 页面 B：新建简历。
 *
 * M0 只建立路由空壳。
 * 「从空白创建」「使用示例数据创建」两个入口属于后续 Milestone。
 */
export default function NewResumePage() {
  return (
    <section className="panel">
      <h1 className="panel__title">新建简历</h1>
      <p className="panel__text">M0 阶段仅建立页面骨架，创建入口尚未实现。</p>
    </section>
  );
}
